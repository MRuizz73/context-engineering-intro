services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - DATABASE_URL=sqlite:////data/gestion_flota.db
    volumes:
      - datos:/data

volumes:
  datos:
